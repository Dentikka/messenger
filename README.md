# Secure Messenger

Безопасный мессенджер с end-to-end шифрованием для Windows.

## 🌟 Особенности

- **End-to-end шифрование** (RSA 2048 + AES-256)
- **Локальное хранение** сообщений в SQLite
- **Графический интерфейс** на PyQt5
- **Гибридная архитектура** (сервер + P2P)
- **Обход NAT** через STUN/Hole Punching
- **Полноценный установщик** для Windows

## 📋 Требования

- Windows 7/8/10/11
- Python 3.8+ (для разработки)
- 50MB свободного места

## 🚀 Установка для разработки

### 1. Клонирование репозитория
```bash
git clone https://github.com/yourusername/secure-messenger.git
cd secure-messenger
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Запуск в режиме разработки
```bash
python main.py
```

## 🏗️ Сборка приложения

### Вариант 1: Полная сборка с установщиком (Рекомендуется)

#### Шаг 1: Создание исполняемого файла
```bash
python setup.py build
```

#### Шаг 2: Подготовка файлов для установщика
```bash
# Создаем папку dist если её нет
mkdir dist

# Копируем файлы из build в dist
xcopy /E /I build\exe.win-amd64-3.9\* dist\
```

#### Шаг 3: Создание установщика с Inno Setup
1. Скачайте и установите [Inno Setup](https://jrsoftware.org/isinfo.php)
2. Откройте файл `installer_script.iss` в Inno Setup Compiler
3. Нажмите "Compile"
4. Получите файл `Output\SecureMessenger-Setup.exe`

### Вариант 2: Только исполняемый файл
```bash
python setup.py build
# Исполняемый файл будет в: build\exe.win-amd64-3.9\SecureMessenger.exe
```

### Вариант 3: MSI установщик (через cx_Freeze)
```bash
python setup.py bdist_msi
# Установщик будет в: dist\SecureMessenger-1.0-win32.msi
```

## 📦 Структура проекта

```
messenger/
├── gui/                    # Графический интерфейс
│   ├── main_window.py      # Главное окно
│   ├── chat_window.py      # Окно чата
│   └── login_dialog.py     # Диалог входа
├── crypto/                 # Шифрование
│   └── encryption.py       # RSA + AES шифрование
├── network/                # Сетевые функции
│   ├── client.py           # Основной клиент (гибридный)
│   ├── server.py           # Центральный сервер
│   ├── p2p_client.py       # P2P клиент
│   ├── stun_client.py      # STUN клиент
│   └── nat_traversal.py    # NAT traversal утилиты
├── storage/                # База данных
│   └── database.py         # SQLite менеджер
├── models/                 # Модели данных
├── config/                 # Конфигурация
├── utils/                  # Утилиты
├── resources/              # Ресурсы (иконки, стили)
├── setup.py               # Скрипт сборки cx_Freeze
├── installer_script.iss   # Скрипт Inno Setup
├── main.py                # Точка входа
└── requirements.txt       # Зависимости
```

## 🎯 Использование

### Запуск сервера (на одном из устройств)
```bash
python -m network.server
```

### Запуск клиента
1. Запустите `SecureMessenger.exe`
2. Введите ваш Peer ID
3. Укажите адрес сервера
4. Нажмите "Connect"

### Добавление контактов
1. В главном окне нажмите "Add Peer"
2. Введите Peer ID контакта
3. Укажите путь к файлу публичного ключа контакта

### Обмен сообщениями
1. Выберите контакт из списка
2. Введите сообщение в поле ввода
3. Нажмите "Send" или Enter

## 🔐 Безопасность

### Шифрование
- **RSA 2048** для обмена симметричными ключами
- **AES-256-CBC** для шифрования сообщений
- **Каждый пользователь** имеет уникальную пару ключей
- **Приватные ключи** хранятся локально и никогда не передаются

### Хранение данных
- **SQLite база данных** для хранения сообщений
- **Все сообщения** хранятся в зашифрованном виде
- **Ключи** хранятся в отдельной папке `data/keys/`
- **Данные пользователя** изолированы в `%APPDATA%`

### Сетевая безопасность
- **Гибридная архитектура**: сервер + P2P
- **Зашифрованные сообщения** передаются через сервер или напрямую
- **Сервер не имеет доступа** к содержимому сообщений
- **Обход NAT** через STUN и Hole Punching

## 🌐 P2P (Peer-to-Peer) архитектура

### Как работает:
1. **STUN** - определение внешнего IP и порта
2. **Hole Punching** - пробивание дыр в NAT
3. **Прямое соединение** между устройствами

### Преимущества P2P:
- ✅ **Без сервера** - сообщения идут напрямую
- ✅ **Быстрее** - меньше задержек
- ✅ **Приватнее** - нет промежуточных серверов
- ✅ **Гибкость** - fallback на сервер при проблемах

### Когда используется:
- При успешном обходе NAT
- Автоматически переключается с сервера на P2P
- Сервер используется как резервный канал

## 🚀 Распространение приложения

### Для конечных пользователей
1. **Распространяйте только установщик:**
   - `SecureMessenger-Setup.exe` (20-50MB)
   - **НЕ распространяйте** папку `dist/` или `build/`

2. **Инструкция для пользователей:**
   ```
   1. Скачайте SecureMessenger-Setup.exe
   2. Запустите установщик (не требует админских прав)
   3. Следуйте инструкциям установщика
   4. Запустите приложение через ярлык
   ```

### Для разработчиков
1. **Git репозиторий содержит:**
   - Исходный код
   - Скрипты сборки
   - Документацию

2. **Исключите из Git:**
   ```
   build/
   dist/
   data/
   *.exe
   *.msi
   __pycache__/
   *.pyc
   ```

## 🛠️ Обновление приложения

### Для пользователей
1. **Полное удаление старой версии:**
   - Панель управления → Программы → Удалить SecureMessenger
   - Или запустить установщик новой версии (он заменит старую)

2. **Установка новой версии:**
   - Запустить новый `SecureMessenger-Setup.exe`
   - Данные и ключи сохраняются

### Для разработчиков
1. **Обновление версии:**
   - Измените версию в `setup.py`
   - Измените версию в `installer_script.iss`
   - Пересоберите приложение

## 📁 Структура установки

### После установки приложение находится:
```
C:\Users\[Username]\AppData\Roaming\SecureMessenger\
├── SecureMessenger.exe     # Исполняемый файл
├── data/                   # Пользовательские данные
│   ├── messages.db         # База данных сообщений
│   └── keys/               # Криптографические ключи
│       ├── private_key.pem # Приватный ключ
│       └── public_key.pem  # Публичный ключ
└── библиотеки и зависимости
```

### Ярлыки создаются:
- **Меню Пуск:** SecureMessenger
- **Рабочий стол:** (опционально)

## ⚠️ Устранение неполадок

### Проблемы с запуском
1. **"Не удается запустить приложение":**
   - Убедитесь, что все файлы скопированы
   - Попробуйте запустить от имени администратора

2. **"Не найдены зависимости":**
   - Переустановите приложение
   - Проверьте антивирус

### Проблемы с сетью
1. **"Не удается подключиться к серверу":**
   - Проверьте адрес и порт сервера
   - Убедитесь, что сервер запущен
   - Проверьте брандмауэр Windows

2. **"P2P соединение не устанавливается":**
   - Сложные типы NAT могут блокировать P2P
   - Приложение автоматически использует сервер как fallback

### Проблемы с ключами
1. **"Не удается расшифровать сообщение":**
   - Убедитесь, что у вас есть публичный ключ контакта
   - Проверьте целостность файлов ключей

## 📄 Лицензия

MIT License - см. файл LICENSE

## 👥 Поддержка

- Создавайте Issues на GitHub
- Обратная связь: your-email@example.com

## 🚀 Быстрый старт для тестирования

### Устройство 1 (Сервер):
```bash
python -m network.server
```

### Устройство 2 (Клиент 1):
1. Запустить `SecureMessenger.exe`
2. Peer ID: `user1`
3. Server: `localhost:8888`

### Устройство 3 (Клиент 2):
1. Запустить `SecureMessenger.exe`
2. Peer ID: `user2`
3. Server: `localhost:8888`

### Обмен ключами:
1. Найти публичные ключи в `data/keys/public_key.pem`
2. Добавить контакты через "Add Peer"
3. Начать общение!

### Тестирование P2P:
1. Запустите клиенты в разных сетях
2. Приложение автоматически попытается установить P2P соединение
3. Если успешно - сообщения пойдут напрямую
4. Если нет - через сервер (fallback)

---
